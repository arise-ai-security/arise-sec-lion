# Pinned config for cell A1 — Claude Code baseline WITH subagents enabled.
# The harness dispatches to experiments.shared.baselines.run_claude_code via
# `run-baseline --variant claude-code-subagent`. This file documents the
# pinned variant and any env overrides; it is NOT consumed by `main.py run`.

variant: claude-code-subagent
notes: |
  Claude Code non-interactive run (`claude -p --output-format stream-json
  --include-partial-messages ...`). The Task tool is allowed, so the model
  can spawn child agents freely.

# Cell B1 — ours-naive.
#
# TODO(Q1): The semantic deltas between B1 (naive), B2 (cybersec), and
# B3 (full) are not yet defined. Until that research question is resolved,
# all three B cells run the same treatment (our hierarchical pipeline with
# the security domain plugin enabled). See `.claude/docs/experiments-rearchitecture.md`
# §7 (Open Questions) and `.claude/docs/experiments-implementation-plan.md`
# (PR 5 — pending Q1 resolution).
#
# When Q1 is answered, this file should encode the cell-specific deltas
# from `config/config.yaml`. Until then, the overlay is intentionally a
# pass-through.

extends: config/config.yaml

overrides:
  orchestration.mode: hierarchical
  worker.tool: openhands
  domain.plugin: security

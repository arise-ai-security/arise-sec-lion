# Pinned config for cell A2 — Claude Code baseline WITHOUT subagents.
# The harness dispatches to
# experiments.shared.baselines.run_claude_code_no_subagent via
# `run-baseline --variant claude-code-nosubagent`. The Task tool is
# disallowed (`--disallowedTools Task`) so the model executes flat.

variant: claude-code-nosubagent
notes: |
  Flat Claude Code run. Compared to A1, this isolates the cost/quality
  effect of hierarchical task decomposition.

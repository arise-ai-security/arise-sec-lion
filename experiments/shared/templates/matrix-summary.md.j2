# Matrix Summary — {{ study_id }}

Rendered at {{ rendered_at }}.

## Totals

- Jobs total: **{{ total_jobs }}**
- Succeeded: **{{ succeeded_count }}**
- Failed: **{{ failed_count }}**
- Replicates per (cell, task): **{{ replicates }}**

## Filters

- Cells: {{ cells_filter }}
- Tasks: {{ tasks_filter }}

## Per-Cell Breakdown

| Cell | Succeeded | Failed |
|------|----------:|-------:|
{%- for row in per_cell %}
| {{ row.cell }} | {{ row.succeeded }} | {{ row.failed }} |
{%- endfor %}

{% if failed_jobs -%}
## Failed Jobs

| Cell | Task | Replicate | Error |
|------|------|----------:|-------|
{%- for job in failed_jobs %}
| {{ job.cell }} | {{ job.task }} | {{ job.replicate }} | {{ job.error }} |
{%- endfor %}
{% else -%}
## Failed Jobs

None — every job succeeded.
{% endif %}

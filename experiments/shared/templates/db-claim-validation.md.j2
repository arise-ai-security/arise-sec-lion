# DB Claim Validation

Generated at: `{{ generated_at }}`

Study: `{{ study_id }}`

Authoritative inputs are the study manifest, dataset, cell configs, enrollment
lockfile, and the Postgres `events` table. JSON run manifests and projected
`events.jsonl` files are not used.

## Summary

Target CVEs from dataset: `{{ target_cve_count }}`

Enrolled runs from lockfile: `{{ enrolled_run_count }}`

{{ summary_table }}

## Reported Claim Check

The reported `237 terminal / 210 success / 121 CVEs attempted` claim is accepted
only where the DB-derived value matches exactly.

{{ claim_table }}

## Invalid And Censored Runs

{{ invalid_reason_table }}

## Orphan DB Aggregates

Orphan DB aggregates not mapped by `enrollment.lock.yaml`: `{{ orphan_count }}`

They are listed in `db_orphan_runs.csv` and excluded from A1/A2 inference unless
a config/lockfile maps them to a cell.

## Generated Files

{% for path in generated_files -%}
- `{{ path }}`
{% endfor %}

## non_authoritative_filesystem_check

Not run. This analysis intentionally does not read JSON run manifests or
projected `events.jsonl` files.

# Dev phase configuration - Supabase hosted database
# Use with: ARISE_ENV=dev

database:
  host: db.waadhvhoendztuvomiob.supabase.co
  port: 6543  # Supabase connection pooler (recommended)
  user: postgres
  name: postgres

output:
  verbose: true
  log_level: DEBUG

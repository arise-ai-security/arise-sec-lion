# Development environment overrides
# Base values come from config.yaml

infrastructure:
  # Use cheaper/faster models for development
  llm_model_boss: gpt-4o-mini

presentation:
  verbose: true
  log_level: DEBUG

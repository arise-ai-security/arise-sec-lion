<operation>
Assess this task. You have reconnaissance tools available — use them to inspect the
codebase before deciding.

**Available tools:**
- `read_file(path)` — read file contents
- `list_directory(path)` — list directory entries
- `search_codebase(pattern, path?)` — grep for a regex pattern across source files
- `find_file(pattern, path?)` — find files matching a glob pattern
- `get_file_structure(path?, max_depth?)` — tree view of directory structure

**Workflow:** Investigate first, then decide.
1. Use tools to understand the scope — check file structure, read key files, search for patterns
2. Based on what you find, choose one of the two options below

**Execute directly** when ALL are true:
- You know exactly which files to modify and what changes to make
- You can verify correctness (expected output, test, or behavior)
- No further exploration or design decisions remain
- A single coding session will complete it

**Decompose** when ANY are true:
- Multiple independent changes across different subsystems
- The task mixes distinct concerns that benefit from parallel work
- The scope is too large for a single worker session
{% if at_max_depth %}

**CONSTRAINT: Maximum depth reached.** You MUST execute directly.
{% elif depth_remaining is not none and depth_remaining <= 1 %}

**Note**: {{ depth_remaining }} level(s) remaining — prefer executing directly.
{% endif %}
{% if agents_remaining is not none and agents_remaining <= 0 %}

**CONSTRAINT: No agents remaining.** You MUST execute directly.
{% endif %}
</operation>
{% if briefing %}

<briefing>
Parent: [{{ briefing.parent_role }}] {{ briefing.parent_task }}
{% if briefing.ancestry %}
Ancestry:
{% for ancestor in briefing.ancestry %}
  - [{{ ancestor.role }}] {{ ancestor.task_summary }}
{% endfor %}
{% endif %}
{% if briefing.decisions %}
Inherited decisions:
{% for decision in briefing.decisions %}
  - {{ decision }}
{% endfor %}
{% endif %}
</briefing>
{% endif %}

<task>
{{ task_description }}
</task>

<output_format>
Return ONLY valid JSON.

To execute directly:
```json
{"action": "execute", "reasoning": "why you're confident (20-500 chars)"}
```

To decompose:
```json
{
  "action": "decompose",
  "reasoning": "what uncertainty requires decomposition (20-500 chars)",
  "subtasks": [
    {
      "description": "concrete subtask (10-500 chars)",
      "justification": {"objective": "what this achieves", "plan": "how to execute"},
      "config": {
        "strategy": "heuristic",
        "base": {"model": "gpt-4o-mini", "temperature": 0.5, "max_tokens": 4000},
        "tool": "{{ default_tool }}"
      },
      "depends_on": [],
      "estimated_complexity": "simple | complex | unknown",
      "success_criteria": "what constitutes success"
    }
  ]
}
```

Rules:
1. Pure JSON only (no markdown, no commentary)
2. `action`: exactly `"execute"` or `"decompose"`
{% if at_max_depth or (agents_remaining is not none and agents_remaining <= 0) %}
3. **FORCED**: You MUST choose `"execute"` (constraint reached)
{% else %}
3. If decomposing: 2-3 subtasks optimal{% if max_subtasks %} (max {{ max_subtasks }}){% endif %}

4. Each subtask should pass the "execute directly" confidence test
{% endif %}
{% if max_subtasks or (agents_remaining is not none and agents_remaining > 0 and agents_remaining <= 10) %}

If constraints cannot be satisfied:
```json
{"status": "constraints_unsatisfiable", "reason": "explanation", "minimum_required": {"subtasks": N, "depth_levels": N}}
```
{% endif %}
</output_format>

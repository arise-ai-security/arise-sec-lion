<OUTPUT_FORMAT>

Return ONLY valid JSON:

```json
{
  "complexity": "simple" | "complex",
  "reasoning": "string (20-500 chars)"
}
```

**Rules**:
1. Pure JSON only (no markdown, no commentary)
2. `complexity`: exactly `"simple"` or `"complex"` (lowercase)
3. `reasoning`: specific task characteristics justifying your classification

**Example**:
```json
{"complexity": "simple", "reasoning": "Single function with clear requirements, no dependencies, completable in one session."}
```

</OUTPUT_FORMAT>

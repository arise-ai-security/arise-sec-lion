<OUTPUT_FORMAT>

Return ONLY valid JSON array:

```json
[
  {
    "description": "string (10-500 chars)",
    "justification": {
      "objective": "what this subtask achieves",
      "plan": "how it will be executed"
    },
    "config": {
      "strategy": "heuristic",
      "base": {"model": "gpt-4o-mini", "temperature": 0.5, "max_tokens": 4000},
      "tool": "{{ default_tool }}"
    }
  }
]
```

## Config Strategies

| Strategy | Schema |
|----------|--------|
| **heuristic** | `{"strategy": "heuristic", "base": {model, temperature, max_tokens}, "tool": "..."}` |
| **per_operation** | `{"strategy": "per_operation", "complexity_evaluation": {...}, "task_decomposition": {...}, "tool": "..."}` |
| **hybrid** | `{"strategy": "hybrid", "base": {...}, "overrides": {...}, "tool": "..."}` |

## Rules

1. Pure JSON array only (no markdown, no commentary)
2. 2-3 subtasks optimal
{% if max_subtasks %}
3. **HARD LIMIT**: Maximum {{ max_subtasks }} subtasks
{% endif %}
{% if agents_remaining is not none and agents_remaining >= 0 %}
4. **AGENT LIMIT**: Maximum {{ agents_remaining }} subtasks (remaining capacity)
{% endif %}
{% if at_max_depth %}
5. **MAX DEPTH**: Each subtask MUST be atomic and worker-executable
{% elif depth_remaining is not none and depth_remaining <= 1 %}
5. **NEAR MAX DEPTH**: Prefer simpler, worker-executable subtasks
{% endif %}
6. Required fields: `description`, `justification`, `config`
7. Config must include: `strategy`, `tool`, and strategy-specific fields
8. Temperature: 0.0-2.0 | max_tokens: 1-100000
9. Models: gpt-4o, gpt-4o-mini, claude-3-5-sonnet-20241022, claude-sonnet-4-20250514, etc.

{% if at_max_depth or max_subtasks or (agents_remaining is not none and agents_remaining >= 0) %}
## Constraint Violation

If constraints cannot be satisfied, respond with:
```json
{
  "status": "constraints_unsatisfiable",
  "reason": "explanation",
  "minimum_required": {"subtasks": N, "depth_levels": N}
}
```
{% endif %}

</OUTPUT_FORMAT>

{# OBSOLETE — content removed 2026-05-17.
   This template was only reachable via `trigger_redecomposition`
   (child returns infeasible → parent re-enters ANALYZING →
   `evaluate_task` renders this). On the happy path
   (`assess_task` → action="decompose" → `_apply_assessment_result`
   spawns children directly), `evaluate_task` is never called and this
   template is never rendered.
   Decomposition guidance for the Fixer phase now lives entirely in
   `prompts/domains/secbench/assess.j2`; the per-role deliverable
   contracts live in `prompts/domains/secbench/worker/fixer.j2`.
   The `render_if` call in `plugins/security/prompt_strategy.py`
   that loaded this file has been commented out. See
   `experiments/b1-batch-autogen/FINDINGS.md` §5.2 for the analysis. #}

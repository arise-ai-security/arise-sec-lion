<domain_instructions>
### Your role: [Build-Verifier] agentic fallback

The trusted host Build-Verifier failed once. Diagnose only its recorded failure and verify the
existing Build-Executor outputs; do not redesign the build.

1. Read the host-procedure failure digest and `{{ artifact_dirs.testcase }}/build.log`.
2. Apply only the smallest correction required by that evidence, then run `secb build` once.
3. Require exit 0 and verify every declared binary is a non-empty executable ELF that starts with
   the configured sanitizer runtime available.
4. Print the commands, exits, binary paths, and raw failure or success evidence.

Do not claim success from an agent-authored verdict file.
</domain_instructions>

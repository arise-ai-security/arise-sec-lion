# Arise Sec Lion
#
# Usage:
#   docker compose --profile local up -d           # Start local dev (with local DB)
#   docker compose --profile dev up -d             # Start dev (Supabase DB, no local DB)
#   docker compose --profile prod up -d            # Start production
#   docker compose --profile test up -d db         # Start the shared local DB for tests

services:
  # --- Local profile (local PostgreSQL) ---
  api:
    profiles: [local]
    build:
      context: ..
      dockerfile: deployment/Dockerfile
      target: development
    container_name: arise-api
    volumes:
      - ..:/app
      - /app/.venv
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      ARISE_INFRA_POSTGRES_HOST: db
    ports:
      - "8000:8000"
    command: uvicorn query.api.bootstrap:create_app --factory --host 0.0.0.0 --port 8000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    profiles: [local]
    init: true  # Use tini to reap zombie processes
    build:
      context: ..
      dockerfile: deployment/Dockerfile
      target: development
    container_name: arise-app
    volumes:
      - ..:/app
      - /app/.venv
      - /var/run/docker.sock:/var/run/docker.sock
      - ../runs:/app/runs  # Host bind mount for SEC-bench artifacts (DooD needs host paths)
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      ARISE_INFRA_POSTGRES_HOST: db
      DOCKER_HOST: unix:///var/run/docker.sock  # Enable DooD for SEC-bench workers
      HOST_PROJECT_ROOT: ${HOST_PROJECT_ROOT}  # Host path for DooD volume mapping
    command: sleep infinity
    stdin_open: true
    tty: true

  web:
    profiles: [local]
    image: node:22-slim
    container_name: arise-web
    volumes:
      - ../query/web:/app
    working_dir: /app
    command: sh -c "npm install && npm run build && npm run dev -- --host 0.0.0.0"

  db:
    profiles: [local, test, prod]
    image: pgvector/pgvector:pg16
    container_name: postgres-main
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ../data/postgres:/var/lib/postgresql/data
      - ../infrastructure/sql/create_events_table.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ./create_local_test_database.sql:/docker-entrypoint-initdb.d/02-test-database.sql:ro
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-arise}
      POSTGRES_HOST_AUTH_METHOD: trust
      POSTGRES_DB: ${POSTGRES_DB:-arise_events}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-arise} -d ${POSTGRES_DB:-arise_events}"]
      interval: 5s
      timeout: 5s
      retries: 5

  # --- Dev profile (Supabase external DB) ---
  api-dev:
    profiles: [dev]
    build:
      context: ..
      dockerfile: deployment/Dockerfile
      target: development
    container_name: arise-api-dev
    volumes:
      - ..:/app
      - /app/.venv
    env_file: .env.dev
    ports:
      - "8000:8000"
    command: uvicorn query.api.bootstrap:create_app --factory --host 0.0.0.0 --port 8000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  app-dev:
    profiles: [dev]
    init: true  # Use tini to reap zombie processes
    build:
      context: ..
      dockerfile: deployment/Dockerfile
      target: development
    container_name: arise-app-dev
    volumes:
      - ..:/app
      - /app/.venv
      - /var/run/docker.sock:/var/run/docker.sock
      - ~/.claude:/home/appuser/.claude  # Mount local Claude config (API key + debug logs)
      - secbench_output:/app/runs  # Shared volume for SEC-bench artifacts (PoC, patches, logs)
    env_file: .env.dev
    environment:
      - DOCKER_HOST=unix:///var/run/docker.sock  # Enable DooD for SEC-bench workers
      - HOST_PROJECT_ROOT=${HOST_PROJECT_ROOT}  # Host path for DooD volume mapping
    command: sleep infinity
    stdin_open: true
    tty: true

  # --- Production profile ---
  app-prod:
    profiles: [prod]
    build:
      context: ..
      dockerfile: deployment/Dockerfile
      target: production
    container_name: arise-app-prod
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      ARISE_INFRA_POSTGRES_HOST: db

volumes:
  secbench_output:  # Shared volume for SEC-bench artifacts between containers
